const http = "http://localhost:8888"
// 接口URL

const apiList = {
	start: http + '/start',
	end: http + '/end',
	draw: http + '/draw',
	drawList: http + '/draw/list',
	appointment: http + '/appointment',
	appointmentList: http + '/appointment/list',
	placeStatistics: http + '/place/statistics'
}

export default apiList