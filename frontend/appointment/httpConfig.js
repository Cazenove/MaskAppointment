const http = "http://localhost:8888"
// 接口URL

const apiList = {
	getStatus: http + '/status',
	getPlace: http + '/place',
	appoint: http + '/appoint',
	query: http + '/query'
}

export default apiList